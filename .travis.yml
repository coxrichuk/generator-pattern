language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-pattern' ]; then cd .. && eval "mv $currentfolder generator-pattern" && cd generator-pattern; fi
